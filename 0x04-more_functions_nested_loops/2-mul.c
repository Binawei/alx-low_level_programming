#include "main.h"
/**
 * mul - multiplies two functions
 * @a: the character that will be used as argument
 * @b: the second character to be used
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	return (a * b);
}
