#include "main.h"
/**
 * set_string - sers the value of a pointer to a character
 * @to: the pointer that will be set ti character
 * @s: the pointer to change
 * Return: nothing
 */
void set_string(char **s, char *to)
{
	*s = to;
}
